#[cfg(test)]
mod tests {
    #[test]
    fn it_works() {
        let result = 2 + 2;
        assert_eq!(result, 4);
    }
}

// pub mod message;
// pub mod user;
pub const MESSAGE_SIZE: usize = 1024;
